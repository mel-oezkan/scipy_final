# ToDo. functionalities to manage and clean the scrapped datasets 
import pandas as pd
import os

data_path = os

def to_image() -> None:
    """ When the user annotates the images the reviewed images
    will be save as png files within the data directory for later
    use, when the network will be trained
    """
    
    pass

def clen_dataset(path):
    # checks if the articles are still online
    # and removes enties which are not

    pass
